default_stages: [pre-commit]
default_install_hook_types:
  [pre-commit, prepare-commit-msg, commit-msg, post-commit, pre-push]

ci:
  skip:
    - mirror-check
    - no-commit-to-branch

repos:
  - repo: https://github.com/George-Ogden/mirror-rorrim/
    rev: v0.4.10
    hooks:
      - id: mirror-check
        fail_fast: true

  - repo: builtin
    hooks:
      - id: no-commit-to-branch
        fail_fast: true
      - id: end-of-file-fixer
        stages: [pre-commit]
      - id: mixed-line-ending
        stages: [pre-commit]
      - id: trailing-whitespace
        stages: [pre-commit]
      - id: check-added-large-files
        stages: [pre-commit]
      - id: check-merge-conflict
        stages: [pre-commit]
        args: [--assume-in-merge]

  - repo: https://github.com/scop/pre-commit-shfmt/
    rev: v3.13.1-1
    hooks:
      - id: shfmt
        stages: [pre-commit]

  - repo: https://github.com/koalaman/shellcheck-precommit/
    rev: v0.11.0
    hooks:
      - id: shellcheck
        args: [--severity=warning]

  - repo: https://github.com/rbubley/mirrors-prettier/
    rev: v3.9.6
    hooks:
      - id: prettier

  - repo: https://github.com/rhysd/actionlint/
    rev: v1.7.12
    hooks:
      - id: actionlint

  - repo: https://github.com/codespell-project/codespell/
    rev: v2.4.3
    hooks:
      - id: codespell
        exclude: \.lock$
        args:
          - -f
          - --regex
          # Copied from https://github.com/codespell-project/codespell/issues/2730#issuecomment-1657006554.
          - |
            (?<![a-zà-öø-ÿ])[a-zà-öø-ÿ'`]+|[A-ZÀ-ÖØ-Þ][a-zà-öø-ÿ'`]*|[a-zà-öø-ÿ]+'[a-zà-öø-ÿ]*|[a-zà-öø-ÿ]+(?=[_-])|[a-zà-öø-ÿ]+(?=[A-ZÀ-ÖØ-Þ])|\d+

  - repo: https://github.com/George-Ogden/pre-commit-hooks/
    rev: v4.1.1
    hooks:
      - id: todo-check
        exclude: (?x)^(
          \.pre-commit-config\.yaml|
          \.gitignore
          )$
      - id: spell-check-commit-msgs
      - id: mirror-auto-msg
      - id: version-txt-auto-msg
      - id: list-untracked-files
