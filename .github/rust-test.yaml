name: Run Rust Tests

on:
  push:
    branches:
      - m
  pull_request:
    branches:
      - m

jobs:
  cargo-test:
    uses: George-Ogden/actions/.github/workflows/rust-test.yaml@v4.6.4
    with:
      rust-version: stable
