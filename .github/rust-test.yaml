name: Run Rust Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  cargo-test:
    uses: George-Ogden/actions/.github/workflows/rust-test.yaml@v4.3.0
    with:
      rust-version: stable
