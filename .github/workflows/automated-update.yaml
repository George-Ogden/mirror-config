name: Automated Update

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: write
  pull-requests: write
  issues: read

jobs:
  update-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current repository
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Clone actions repository
        run: |
          git clone https://github.com/George-Ogden/actions.git /tmp/external-repo

      - name: Read new version
        run: |
          NEW_VERSION=$(cat /tmp/external-repo/version.txt)
          echo "New version: '$NEW_VERSION'"
          echo "VERSION=${NEW_VERSION}" >> "$GITHUB_ENV"

      - name: Update action files
        run: |
          find .github -name "*.yaml" -exec sed -i "/George-Ogden\/actions\/.github\/workflows/s/@.*$/@v${{ env.VERSION }}/" {} \;

      - name: Create PR
        uses: ./.github/actions/commit-and-pr
        with:
          branch: "update-version-${{ env.VERSION }}"
          commit-message: "Automated actions version update"
          pr-title: "Automated Actions Version Sync"
          pr-body: "Update actions version to ${{ env.VERSION }}"
          token: ${{ secrets.PAT_TOKEN }}

  autoupdate-pre-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Python dependencies
        run: |
          pip install uv
          uv pip install pyyaml --system
          uv tool install prek

      - name: Run pre-commit autoupdate
        run: |
          prek autoupdate
          prek autoupdate -c python.pre-commit-config.yaml
          prek autoupdate -c rust.pre-commit-config.yaml
          prek autoupdate -c text.pre-commit-config.yaml

      - name: Update Ruff version in requirements-dev.txt
        run: |
          RUFF_VERSION=$(python - <<'EOF'
          import yaml
          with open("python.pre-commit-config.yaml") as f:
              config = yaml.safe_load(f)
          [ruff_repo] = [repo for repo in config["repos"] if repo["repo"] == "https://github.com/astral-sh/ruff-pre-commit/"]
          version = ruff_repo["rev"]
          assert version.startswith("v")
          print(version.replace("v", "", count=1))
          EOF
          )

          sed -i "s/^ruff=.*\$/ruff==$RUFF_VERSION/" requirements-dev.txt

      - name: Create PR
        uses: ./.github/actions/commit-and-pr
        with:
          branch: pre-commit-autoupdate
          commit-message: "Automated pre-commit autoupdate"
          pr-title: "Automated Pre-Commit Autoupdate"
          pr-body: "Autoupdate pre-commit"
          token: ${{ secrets.PAT_TOKEN }}

  update-mypy-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Update Mypy version in requirements-dev.txt
        run: |
          MYPY_VERSION=$(pip index versions --json mypy | jq -r '.latest' | cat)
          echo "VERSION=${MYPY_VERSION}" >> "$GITHUB_ENV"
          sed -i "s/^mypy=.*/mypy==$MYPY_VERSION/" requirements-dev.txt

      - name: Create PR
        uses: ./.github/actions/commit-and-pr
        with:
          branch: "update-mypy-version-${{ env.VERSION }}"
          commit-message: "Automated mypy version update"
          pr-title: "Automated Mypy Version Sync"
          pr-body: "Update mypy version to ${{ env.VERSION }}"
          token: ${{ secrets.PAT_TOKEN }}
